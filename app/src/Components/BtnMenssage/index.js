import styled from 'styled-components';

const BtnMessage = styled.input.attrs({
    placeholder:'Diz aí...'
})`
    width: 375px;
    height: 50px;
    margin-top:50px;
`;

export default BtnMessage;